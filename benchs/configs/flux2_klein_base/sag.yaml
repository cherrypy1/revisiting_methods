# FLUX.2-klein-4B, Self-Attention Guidance + CFG.
pipeline: pipelines.flux2_klein_base.sag
generation_params:
  num_inference_steps: 30
  guidance_scale: 4.5
  sag_scale: 1.5
  sag_blur_sigma: 2.0
  sag_applied_layers: ["d4", "s1"]
