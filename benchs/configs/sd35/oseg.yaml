# SD3.5-medium, OSEG (orthogonalised SEG, Fahim 2026 WACV) + CFG.
pipeline: pipelines.sd35.oseg
generation_params:
  num_inference_steps: 25
  guidance_scale: 3.0
  seg_scale: 2.0
  oseg_scale: 0.5
  seg_blur_sigma: 1.0e10
  seg_applied_layers: ["d8"]
