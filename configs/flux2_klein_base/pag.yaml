# FLUX.2-klein-4B, Perturbed Attention Guidance.
pipeline: pipelines.flux2_klein_base.pag
generation_params:
  num_inference_steps: 30
  guidance_scale: 4.5
  pag_scale: 1.5
  pag_applied_layers: ["d4", "s1"]
