# FLUX.2-klein-4B, SEG + CFG.
pipeline: pipelines.flux2_klein_base.seg
generation_params:
  num_inference_steps: 30
  guidance_scale: 4.0
  seg_scale: 3.0
  seg_blur_sigma: 1.0e10
  seg_applied_layers: ["d4", "s1"]
