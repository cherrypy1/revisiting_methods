# SD3.5-medium, CFG-Zero* (Fang et al. 2025).
# zero_steps=0 → 1 zero-init step (impl uses `i <= zero_steps`).
pipeline: pipelines.sd35.cfg0s
generation_params:
  num_inference_steps: 25
  guidance_scale: 6.5
  use_zero_init: true
  zero_steps: 0
