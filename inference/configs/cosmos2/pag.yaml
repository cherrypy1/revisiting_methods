# Cosmos-Predict2-2B, Perturbed Attention Guidance.
# Block 1 picked by analogy with SD3.5 PAG sweep; Cosmos-2B has 28 blocks.
# May need its own ablation when full sweep runs.
pipeline: pipelines.cosmos2.pag
generation_params:
  height: 1024
  width: 1024
  num_inference_steps: 35
  guidance_scale: 5.5
  method: pag
  pag_scale: 3.0
  pag_applied_layers: [1]
