# Cosmos-Predict2-2B, Self-Attention Guidance + CFG.
pipeline: pipelines.cosmos2.sag
generation_params:
  height: 1024
  width: 1024
  num_inference_steps: 35
  guidance_scale: 5.0
  method: sag
  sag_scale: 0.4
  sag_blur_sigma: 1.0
  sag_applied_layers: [0]
