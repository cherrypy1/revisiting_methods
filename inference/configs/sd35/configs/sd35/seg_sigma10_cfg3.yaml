# SD3.5-medium, SEG (sigma=10) + CFG (scale=3). Hong et al. 2024.
pipeline: pipelines.sd35.seg
generation_params:
  method: seg
  num_inference_steps: 25
  guidance_scale: 3.0
  seg_scale: 3.0
  seg_blur_sigma: 10.0
  seg_applied_layers: ["d8"]
